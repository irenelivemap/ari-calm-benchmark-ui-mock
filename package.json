{
  "name": "ari-route-benchmark-ui-mock",
  "version": "0.0.0",
  "private": true,
  "description": "Static UI prototype for blinded ARI route-comparison benchmarks.",
  "scripts": {
    "start": "python3 -m http.server 8765 --bind 127.0.0.1",
    "test": "node --test tests/*.test.js",
    "check": "npm test"
  },
  "engines": {
    "node": ">=18"
  }
}
