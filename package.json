{
  "name": "ari-route-benchmark-ui-mock",
  "version": "0.0.0",
  "private": true,
  "description": "Static UI prototype for blinded ARI route-comparison benchmarks.",
  "scripts": {
    "start": "node scripts/dev-live.mjs",
    "start:live": "node scripts/dev-live.mjs",
    "test": "node --test tests/*.test.js",
    "check": "npm test"
  },
  "engines": {
    "node": ">=18"
  }
}
