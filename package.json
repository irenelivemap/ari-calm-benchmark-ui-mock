{
  "name": "ari-route-benchmark-ui",
  "version": "0.0.0",
  "private": true,
  "description": "Static participant UI for blinded Ari route-comparison benchmarks.",
  "scripts": {
    "start": "node scripts/dev-live.mjs",
    "start:live": "node scripts/dev-live.mjs",
    "audit:data": "node scripts/audit-benchmark-data.mjs",
    "smoke:production": "node scripts/production-smoke.mjs",
    "test": "node --test tests/*.test.js",
    "test:e2e": "playwright test",
    "check": "npm test && npm run test:e2e"
  },
  "engines": {
    "node": "24.x"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
